{
  "t3code": {
    "sourcePath": "./resources/t3code",
    "entrypoint": "apps/server/dist/index.mjs",
    "installCommand": "bun install --frozen-lockfile",
    "buildCommand": "bun run --cwd apps/web build && bun run --cwd apps/server build"
  }
}
